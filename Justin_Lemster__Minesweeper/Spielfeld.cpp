#include <iostream>
#include<stdio.h>
#include<math.h>
#include "Spielfeld.h"

Spielfeld::Spielfeld() {
}

Spielfeld::Spielfeld(int x, int y, int m) {
	int m_laengeX = x;
	int m_laengeY = y;
	int m_MinenAnz = m;

	/*int num_elements = 5;
	int* ptr = malloc(num_elements * sizeof(*ptr));
	ptr[3] = 123;
	free(ptr);

	char Feld[x][y];*/
}

void Spielfeld::Ausgabe() {

}
